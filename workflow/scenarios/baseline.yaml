# Baseline scenario — no overrides, uses base simulation config.
# simulation:
#   model: gpt-4o-mini
#   rate_limit_rpm: 500   # requests per minute; override per model/tier
#   prompts:
#     persona:
#       role: |
#         ...
